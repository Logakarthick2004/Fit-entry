import { t as supabase } from "./client-BmkySxaU.js";
import { n as useActiveTenant } from "./use-tenant-L5ZosG5R.js";
import { n as PageBody, r as PageHeader } from "./page-header-9A-c6Jmg.js";
import { t as Button } from "./button-BTGd3p39.js";
import { t as Input } from "./input-C7-F0e9p.js";
import { t as Label } from "./label-Cj5oJoSC.js";
import { a as DialogTitle, i as DialogHeader, n as DialogContent, o as DialogTrigger, r as DialogFooter, t as Dialog } from "./dialog-1f98Mygr.js";
import { a as SelectValue, i as SelectTrigger, n as SelectContent, r as SelectItem, t as Select } from "./select-C8RpZWXk.js";
import { useState } from "react";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
import { Plus } from "lucide-react";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { z } from "zod";
//#region src/routes/_authenticated/leads.tsx?tsr-split=component
var STAGES = [
	"new",
	"contacted",
	"interested",
	"trial",
	"converted",
	"lost"
];
var schema = z.object({
	name: z.string().min(2),
	phone: z.string().optional(),
	email: z.string().email().optional().or(z.literal("")),
	source: z.string().optional(),
	notes: z.string().optional()
});
function Leads() {
	const { active } = useActiveTenant();
	const tenantId = active?.tenant_id;
	const [open, setOpen] = useState(false);
	const qc = useQueryClient();
	const { register, handleSubmit, reset, formState: { errors } } = useForm({ resolver: zodResolver(schema) });
	const { data } = useQuery({
		enabled: !!tenantId,
		queryKey: ["leads", tenantId],
		queryFn: async () => {
			const { data } = await supabase.from("leads").select("*").eq("tenant_id", tenantId).order("created_at", { ascending: false });
			return data ?? [];
		}
	});
	const create = useMutation({
		mutationFn: async (v) => {
			if (!tenantId) throw new Error("No tenant");
			const { error } = await supabase.from("leads").insert({
				...v,
				tenant_id: tenantId,
				email: v.email || null
			});
			if (error) throw error;
		},
		onSuccess: () => {
			toast.success("Lead added");
			reset();
			setOpen(false);
			qc.invalidateQueries({ queryKey: ["leads"] });
		},
		onError: (e) => toast.error(e.message)
	});
	const update = useMutation({
		mutationFn: async ({ id, status }) => {
			const { error } = await supabase.from("leads").update({ status }).eq("id", id);
			if (error) throw error;
		},
		onSuccess: () => qc.invalidateQueries({ queryKey: ["leads"] })
	});
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(PageHeader, {
		title: "Leads",
		description: "Sales pipeline",
		actions: /* @__PURE__ */ jsxs(Dialog, {
			open,
			onOpenChange: setOpen,
			children: [/* @__PURE__ */ jsx(DialogTrigger, {
				asChild: true,
				children: /* @__PURE__ */ jsxs(Button, { children: [/* @__PURE__ */ jsx(Plus, { className: "mr-2 h-4 w-4" }), " Add lead"] })
			}), /* @__PURE__ */ jsxs(DialogContent, { children: [/* @__PURE__ */ jsx(DialogHeader, { children: /* @__PURE__ */ jsx(DialogTitle, { children: "New lead" }) }), /* @__PURE__ */ jsxs("form", {
				onSubmit: handleSubmit((v) => create.mutate(v)),
				className: "space-y-3",
				children: [
					/* @__PURE__ */ jsxs("div", { children: [
						/* @__PURE__ */ jsx(Label, { children: "Name *" }),
						/* @__PURE__ */ jsx(Input, { ...register("name") }),
						errors.name && /* @__PURE__ */ jsx("p", {
							className: "text-xs text-destructive",
							children: errors.name.message
						})
					] }),
					/* @__PURE__ */ jsxs("div", {
						className: "grid grid-cols-2 gap-3",
						children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "Phone" }), /* @__PURE__ */ jsx(Input, { ...register("phone") })] }), /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "Email" }), /* @__PURE__ */ jsx(Input, {
							type: "email",
							...register("email")
						})] })]
					}),
					/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "Source" }), /* @__PURE__ */ jsx(Input, {
						...register("source"),
						placeholder: "Instagram, Walk-in…"
					})] }),
					/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "Notes" }), /* @__PURE__ */ jsx(Input, { ...register("notes") })] }),
					/* @__PURE__ */ jsx(DialogFooter, { children: /* @__PURE__ */ jsx(Button, {
						type: "submit",
						children: "Save"
					}) })
				]
			})] })]
		})
	}), /* @__PURE__ */ jsx(PageBody, { children: /* @__PURE__ */ jsx("div", {
		className: "grid gap-3 md:grid-cols-3 lg:grid-cols-6",
		children: STAGES.map((s) => {
			const items = (data ?? []).filter((l) => l.status === s);
			return /* @__PURE__ */ jsxs("div", {
				className: "rounded-xl border border-border bg-card",
				children: [/* @__PURE__ */ jsx("div", {
					className: "border-b border-border p-3",
					children: /* @__PURE__ */ jsxs("h3", {
						className: "font-display text-sm uppercase tracking-wider",
						children: [
							s,
							" ",
							/* @__PURE__ */ jsxs("span", {
								className: "ml-1 text-xs text-muted-foreground",
								children: [
									"(",
									items.length,
									")"
								]
							})
						]
					})
				}), /* @__PURE__ */ jsxs("div", {
					className: "max-h-[60vh] space-y-2 overflow-auto p-2",
					children: [items.map((l) => /* @__PURE__ */ jsxs("div", {
						className: "rounded-md border border-border bg-secondary/40 p-2 text-sm",
						children: [
							/* @__PURE__ */ jsx("div", {
								className: "font-medium",
								children: l.name
							}),
							/* @__PURE__ */ jsx("div", {
								className: "text-xs text-muted-foreground",
								children: l.phone || l.email || "—"
							}),
							/* @__PURE__ */ jsxs(Select, {
								value: l.status,
								onValueChange: (v) => update.mutate({
									id: l.id,
									status: v
								}),
								children: [/* @__PURE__ */ jsx(SelectTrigger, {
									className: "mt-2 h-7 text-xs",
									children: /* @__PURE__ */ jsx(SelectValue, {})
								}), /* @__PURE__ */ jsx(SelectContent, { children: STAGES.map((st) => /* @__PURE__ */ jsx(SelectItem, {
									value: st,
									children: st
								}, st)) })]
							})
						]
					}, l.id)), items.length === 0 && /* @__PURE__ */ jsx("p", {
						className: "p-2 text-center text-xs text-muted-foreground",
						children: "Empty"
					})]
				})]
			}, s);
		})
	}) })] });
}
//#endregion
export { Leads as component };
