import { t as supabase } from "./client-BmkySxaU.js";
import { n as useActiveTenant } from "./use-tenant-L5ZosG5R.js";
import { n as PageBody, r as PageHeader } from "./page-header-9A-c6Jmg.js";
import { n as formatCurrency, t as cn } from "./utils-DMmnJHxL.js";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
import { AlertCircle, Building2, CalendarCheck, Clock, DollarSign, UserCheck, UserCog, Users } from "lucide-react";
import { motion } from "framer-motion";
import { Area, AreaChart, Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
//#region src/hooks/use-dashboard.ts
function useDashboardStats() {
	const { active } = useActiveTenant();
	const tenantId = active?.tenant_id;
	return useQuery({
		enabled: !!tenantId,
		queryKey: ["dashboard-stats", tenantId],
		queryFn: async () => {
			if (!tenantId) throw new Error("No tenant");
			const today = /* @__PURE__ */ new Date();
			today.setHours(0, 0, 0, 0);
			const monthStart = new Date(today.getFullYear(), today.getMonth(), 1);
			const [members, active_mm, expired_mm, attendance_today, payments_month, trainers, branches] = await Promise.all([
				supabase.from("members").select("id", {
					count: "exact",
					head: true
				}).eq("tenant_id", tenantId),
				supabase.from("member_memberships").select("id", {
					count: "exact",
					head: true
				}).eq("tenant_id", tenantId).eq("status", "active"),
				supabase.from("member_memberships").select("id", {
					count: "exact",
					head: true
				}).eq("tenant_id", tenantId).eq("status", "expired"),
				supabase.from("attendance").select("id", {
					count: "exact",
					head: true
				}).eq("tenant_id", tenantId).gte("check_in", today.toISOString()),
				supabase.from("payments").select("amount").eq("tenant_id", tenantId).eq("status", "paid").gte("paid_at", monthStart.toISOString()),
				supabase.from("trainers").select("id", {
					count: "exact",
					head: true
				}).eq("tenant_id", tenantId).eq("is_active", true),
				supabase.from("branches").select("id", {
					count: "exact",
					head: true
				}).eq("tenant_id", tenantId)
			]);
			const revenue = (payments_month.data ?? []).reduce((s, p) => s + Number(p.amount || 0), 0);
			const trendStart = new Date(today.getFullYear(), today.getMonth() - 5, 1);
			const { data: trend } = await supabase.from("payments").select("amount, paid_at").eq("tenant_id", tenantId).eq("status", "paid").gte("paid_at", trendStart.toISOString());
			const monthBuckets = {};
			for (let i = 0; i < 6; i++) {
				const d = new Date(today.getFullYear(), today.getMonth() - (5 - i), 1);
				monthBuckets[`${d.getFullYear()}-${d.getMonth()}`] = 0;
			}
			(trend ?? []).forEach((p) => {
				const d = new Date(p.paid_at);
				const k = `${d.getFullYear()}-${d.getMonth()}`;
				if (monthBuckets[k] !== void 0) monthBuckets[k] += Number(p.amount || 0);
			});
			const revenueTrend = Object.entries(monthBuckets).map(([k, v]) => {
				const [y, m] = k.split("-").map(Number);
				return {
					month: new Date(y, m, 1).toLocaleDateString("en-US", { month: "short" }),
					revenue: Math.round(v)
				};
			});
			const sevenStart = new Date(today);
			sevenStart.setDate(sevenStart.getDate() - 6);
			const { data: att } = await supabase.from("attendance").select("check_in").eq("tenant_id", tenantId).gte("check_in", sevenStart.toISOString());
			const dayBuckets = {};
			for (let i = 0; i < 7; i++) {
				const d = new Date(today);
				d.setDate(d.getDate() - (6 - i));
				dayBuckets[d.toISOString().slice(0, 10)] = 0;
			}
			(att ?? []).forEach((a) => {
				const k = new Date(a.check_in).toISOString().slice(0, 10);
				if (dayBuckets[k] !== void 0) dayBuckets[k] += 1;
			});
			const attendanceTrend = Object.entries(dayBuckets).map(([d, v]) => ({
				day: new Date(d).toLocaleDateString("en-US", { weekday: "short" }),
				checkins: v
			}));
			const { data: dues } = await supabase.from("payments").select("amount").eq("tenant_id", tenantId).eq("status", "pending");
			const outstanding = (dues ?? []).reduce((s, p) => s + Number(p.amount || 0), 0);
			return {
				totalMembers: members.count ?? 0,
				activeMembers: active_mm.count ?? 0,
				expiredMembers: expired_mm.count ?? 0,
				todayCheckins: attendance_today.count ?? 0,
				monthlyRevenue: revenue,
				outstanding,
				trainers: trainers.count ?? 0,
				branches: branches.count ?? 0,
				revenueTrend,
				attendanceTrend
			};
		}
	});
}
//#endregion
//#region src/components/stat-card.tsx
function StatCard({ label, value, icon: Icon, hint, accent }) {
	const tone = accent ?? "primary";
	return /* @__PURE__ */ jsxs(motion.div, {
		initial: {
			opacity: 0,
			y: 8
		},
		animate: {
			opacity: 1,
			y: 0
		},
		className: "rounded-xl border border-border bg-card p-5",
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "flex items-start justify-between",
				children: [/* @__PURE__ */ jsx("span", {
					className: "text-xs uppercase tracking-wider text-muted-foreground",
					children: label
				}), /* @__PURE__ */ jsx("span", {
					className: cn("grid h-9 w-9 place-items-center rounded-md", tone === "primary" && "bg-primary/15 text-primary", tone === "accent" && "bg-accent/15 text-accent", tone === "success" && "bg-success/15 text-success", tone === "warning" && "bg-warning/15 text-warning", tone === "destructive" && "bg-destructive/15 text-destructive"),
					children: /* @__PURE__ */ jsx(Icon, { className: "h-5 w-5" })
				})]
			}),
			/* @__PURE__ */ jsx("div", {
				className: "mt-3 kpi-number",
				children: value
			}),
			hint && /* @__PURE__ */ jsx("div", {
				className: "mt-1 text-xs text-muted-foreground",
				children: hint
			})
		]
	});
}
//#endregion
//#region src/routes/_authenticated/dashboard.tsx?tsr-split=component
function Dashboard() {
	const { active } = useActiveTenant();
	const { data, isLoading } = useDashboardStats();
	const currency = active?.tenant.currency ?? "USD";
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(PageHeader, {
		title: "Dashboard",
		description: `Welcome back to ${active?.tenant.name ?? ""}`
	}), /* @__PURE__ */ jsxs(PageBody, { children: [
		/* @__PURE__ */ jsxs("div", {
			className: "grid gap-4 sm:grid-cols-2 lg:grid-cols-4",
			children: [
				/* @__PURE__ */ jsx(StatCard, {
					label: "Total members",
					value: data?.totalMembers ?? "—",
					icon: Users,
					accent: "primary"
				}),
				/* @__PURE__ */ jsx(StatCard, {
					label: "Active",
					value: data?.activeMembers ?? "—",
					icon: UserCheck,
					accent: "success"
				}),
				/* @__PURE__ */ jsx(StatCard, {
					label: "Expired",
					value: data?.expiredMembers ?? "—",
					icon: Clock,
					accent: "warning"
				}),
				/* @__PURE__ */ jsx(StatCard, {
					label: "Check-ins today",
					value: data?.todayCheckins ?? "—",
					icon: CalendarCheck,
					accent: "accent"
				}),
				/* @__PURE__ */ jsx(StatCard, {
					label: "MTD revenue",
					value: data ? formatCurrency(data.monthlyRevenue, currency) : "—",
					icon: DollarSign,
					accent: "primary"
				}),
				/* @__PURE__ */ jsx(StatCard, {
					label: "Outstanding",
					value: data ? formatCurrency(data.outstanding, currency) : "—",
					icon: AlertCircle,
					accent: "destructive"
				}),
				/* @__PURE__ */ jsx(StatCard, {
					label: "Trainers",
					value: data?.trainers ?? "—",
					icon: UserCog,
					accent: "accent"
				}),
				/* @__PURE__ */ jsx(StatCard, {
					label: "Branches",
					value: data?.branches ?? "—",
					icon: Building2,
					accent: "primary"
				})
			]
		}),
		/* @__PURE__ */ jsxs("div", {
			className: "mt-6 grid gap-4 lg:grid-cols-2",
			children: [/* @__PURE__ */ jsxs("div", {
				className: "rounded-xl border border-border bg-card p-5",
				children: [/* @__PURE__ */ jsxs("div", {
					className: "flex items-baseline justify-between",
					children: [/* @__PURE__ */ jsx("h3", {
						className: "font-display text-xl tracking-wide",
						children: "REVENUE TREND"
					}), /* @__PURE__ */ jsx("span", {
						className: "text-xs text-muted-foreground",
						children: "Last 6 months"
					})]
				}), /* @__PURE__ */ jsx("div", {
					className: "mt-4 h-64",
					children: /* @__PURE__ */ jsx(ResponsiveContainer, {
						width: "100%",
						height: "100%",
						children: /* @__PURE__ */ jsxs(AreaChart, {
							data: data?.revenueTrend ?? [],
							children: [
								/* @__PURE__ */ jsx("defs", { children: /* @__PURE__ */ jsxs("linearGradient", {
									id: "rev",
									x1: "0",
									y1: "0",
									x2: "0",
									y2: "1",
									children: [/* @__PURE__ */ jsx("stop", {
										offset: "0%",
										stopColor: "var(--color-primary)",
										stopOpacity: .5
									}), /* @__PURE__ */ jsx("stop", {
										offset: "100%",
										stopColor: "var(--color-primary)",
										stopOpacity: 0
									})]
								}) }),
								/* @__PURE__ */ jsx(CartesianGrid, {
									strokeDasharray: "3 3",
									stroke: "var(--color-border)"
								}),
								/* @__PURE__ */ jsx(XAxis, {
									dataKey: "month",
									stroke: "var(--color-muted-foreground)",
									fontSize: 12
								}),
								/* @__PURE__ */ jsx(YAxis, {
									stroke: "var(--color-muted-foreground)",
									fontSize: 12
								}),
								/* @__PURE__ */ jsx(Tooltip, { contentStyle: {
									background: "var(--color-card)",
									border: "1px solid var(--color-border)",
									borderRadius: 8
								} }),
								/* @__PURE__ */ jsx(Area, {
									type: "monotone",
									dataKey: "revenue",
									stroke: "var(--color-primary)",
									fill: "url(#rev)",
									strokeWidth: 2
								})
							]
						})
					})
				})]
			}), /* @__PURE__ */ jsxs("div", {
				className: "rounded-xl border border-border bg-card p-5",
				children: [/* @__PURE__ */ jsxs("div", {
					className: "flex items-baseline justify-between",
					children: [/* @__PURE__ */ jsx("h3", {
						className: "font-display text-xl tracking-wide",
						children: "CHECK-INS"
					}), /* @__PURE__ */ jsx("span", {
						className: "text-xs text-muted-foreground",
						children: "Last 7 days"
					})]
				}), /* @__PURE__ */ jsx("div", {
					className: "mt-4 h-64",
					children: /* @__PURE__ */ jsx(ResponsiveContainer, {
						width: "100%",
						height: "100%",
						children: /* @__PURE__ */ jsxs(BarChart, {
							data: data?.attendanceTrend ?? [],
							children: [
								/* @__PURE__ */ jsx(CartesianGrid, {
									strokeDasharray: "3 3",
									stroke: "var(--color-border)"
								}),
								/* @__PURE__ */ jsx(XAxis, {
									dataKey: "day",
									stroke: "var(--color-muted-foreground)",
									fontSize: 12
								}),
								/* @__PURE__ */ jsx(YAxis, {
									stroke: "var(--color-muted-foreground)",
									fontSize: 12,
									allowDecimals: false
								}),
								/* @__PURE__ */ jsx(Tooltip, { contentStyle: {
									background: "var(--color-card)",
									border: "1px solid var(--color-border)",
									borderRadius: 8
								} }),
								/* @__PURE__ */ jsx(Bar, {
									dataKey: "checkins",
									fill: "var(--color-accent)",
									radius: [
										4,
										4,
										0,
										0
									]
								})
							]
						})
					})
				})]
			})]
		}),
		isLoading && /* @__PURE__ */ jsx("p", {
			className: "mt-6 text-sm text-muted-foreground",
			children: "Loading…"
		})
	] })] });
}
//#endregion
export { Dashboard as component };
