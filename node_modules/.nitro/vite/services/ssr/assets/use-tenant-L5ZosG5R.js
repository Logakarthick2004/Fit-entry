import { t as supabase } from "./client-BmkySxaU.js";
import { useQuery } from "@tanstack/react-query";
//#region src/hooks/use-tenant.ts
var ACTIVE_KEY = "fitflow.active_tenant";
function setActiveTenantId(id) {
	if (typeof window !== "undefined") localStorage.setItem(ACTIVE_KEY, id);
}
function useMyTenants() {
	return useQuery({
		queryKey: ["my-tenants"],
		queryFn: async () => {
			const { data: user } = await supabase.auth.getUser();
			if (!user.user) return [];
			const { data, error } = await supabase.from("user_tenants").select("tenant_id, is_default, tenants(id, name, slug, currency, timezone, status, logo_url, tax_percent)").eq("user_id", user.user.id);
			if (error) throw error;
			const { data: roles } = await supabase.from("user_roles").select("tenant_id, role").eq("user_id", user.user.id);
			return (data ?? []).map((r) => ({
				tenant_id: r.tenant_id,
				is_default: r.is_default,
				tenant: r.tenants,
				roles: (roles ?? []).filter((x) => x.tenant_id === r.tenant_id || x.tenant_id === null).map((x) => x.role)
			}));
		}
	});
}
function useActiveTenant() {
	const { data: tenants, isLoading } = useMyTenants();
	const activeId = typeof window !== "undefined" ? localStorage.getItem(ACTIVE_KEY) : null;
	return {
		active: tenants?.find((t) => t.tenant_id === activeId) ?? tenants?.find((t) => t.is_default) ?? tenants?.[0],
		tenants: tenants ?? [],
		isLoading
	};
}
//#endregion
export { useActiveTenant as n, useMyTenants as r, setActiveTenantId as t };
